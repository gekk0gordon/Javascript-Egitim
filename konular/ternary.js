// TERNARY OPERATOR
/*
 koşul ? ifade 1 : ifade 2 ;
 koşul sağlanırsa ifade 1 calısır kosul sağlanmaz ise ifade 2 calısır.
 if else in tek satırda yazılan tipidir.

*/

console.log(1<5 ? "doğru" : "hatali") // 1 5 den kücük olduığu için ?'den sonraki ilk ifade calıstırıldı.

// değişkene ataayarak kullanımı

let kontrol = "5"===5 ? "eşit" : "eşit değil";
console.log(kontrol)